# Session signing secret — generate with: openssl rand -base64 32
AUTH_SECRET=change-me-to-a-long-random-string

# Quiz login (override in production)
QUIZ_USERNAME=ksunilrai
QUIZ_PASSWORD=Cristina,1
